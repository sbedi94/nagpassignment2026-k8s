apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: bookstore
data:
  # Database host — uses the Kubernetes Service name (NOT a pod IP)
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "appdb"
  DB_USER: "appuser"
